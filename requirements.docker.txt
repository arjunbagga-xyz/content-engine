# Minimal runtime deps actually imported by src/ (verified via grep).
# Drops unused heavy packages from requirements.txt:
#   sentence-transformers (+torch ~1GB), rembg (+onnx/torch), chromadb (+torch),
#   opencv-python-headless, moviepy, pytrends, anthropic.
# Keep these only if you enable the dashboard / IG+X publishing.
openai>=1.50.0
Pillow==10.2.0
edge-tts>=7.0.0
instagrapi==1.17.6
tweepy==4.14.0
SQLAlchemy==2.0.28
pyyaml==6.0.1
python-dotenv==1.0.1
requests==2.31.0
# Dashboard (optional but imported by src/dashboard/app.py)
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
jinja2>=3.1.2
aiofiles>=23.1.0
